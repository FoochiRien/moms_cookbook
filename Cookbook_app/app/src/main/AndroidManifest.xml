<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.adrienne.cookbook_app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_cbk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".RecipeDetail.RecipeDetailActivity">
        </activity>
        <activity android:name=".RecipeDetail.WebActivity">
        </activity>
        <activity android:name=".Add_recipe.ManualEnterRecipeActivity">
        </activity>
        <activity android:name=".Add_recipe.AddRecipeByPhotoActivity">
        </activity>
        <activity android:name=".Search.ApiResultDetailActivity">
        </activity>
    </application>

</manifest>